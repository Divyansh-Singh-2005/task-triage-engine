# Core
pydantic>=2.7
pydantic-settings>=2.2
sqlalchemy>=2.0
streamlit>=1.35

# Testing
pytest>=8.0

# Optional LLM providers. Without any of these the offline heuristic baseline
# is used. Ollama needs no package - it is called over plain HTTP.
# anthropic>=0.40
# openai>=1.40
