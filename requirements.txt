# Core
pydantic>=2.7
pydantic-settings>=2.2
sqlalchemy>=2.0
streamlit>=1.35

# Testing
pytest>=8.0

# Optional: real LLM provider. Without it the offline heuristic baseline is used.
# anthropic>=0.40
